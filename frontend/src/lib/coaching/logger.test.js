import { describe,it,expect } from 'vitest'; import { assignedWorkoutFromSnapshot, resultPayload } from './logger.js';
describe('assigned logger adapter',()=>{it('keeps targets separate from actual values and retains skipped exercises',()=>{const snap={unit:'lb',exercises:[{entryId:'entry-a',exerciseId:'a',name:'A',restSec:45,notes:'Controlled',sets:[{weight:10,reps:8}]},{entryId:'entry-b',exerciseId:'b',name:'B',sets:[{weight:20,reps:5}]}]};const active=assignedWorkoutFromSnapshot('s1',snap);active.entries[0].sets[0]={w:12,r:7,done:true};const out=resultPayload(active,'2026-09-07T00:00:00Z');expect(out.actualWorkout.entries[0].sets[0].w).toBe(12);expect(active.assignedPrescription.exercises[0].sets[0].weight).toBe(10);expect(active.assignedPrescription.exercises).toHaveLength(2);expect(active.entries[0]).toMatchObject({id:'a',entryId:'entry-a',target:{restSec:45,notes:'Controlled'}});expect(active.unit).toBe('lb')})});
