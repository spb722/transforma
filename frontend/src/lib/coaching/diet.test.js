import {describe,it,expect} from 'vitest';import {newDiet,normalizeDiet} from './diet.js';describe('diet drafts',()=>{it('keeps meal order and portion text without inventing adherence',()=>{const d=newDiet();d.meals.push({id:'2',name:'Dinner',items:[{name:'Rice',portion:'one bowl'}]});const out=normalizeDiet(d);expect(out.meals.map(x=>x.name)).toEqual(['Breakfast','Dinner']);expect(out.meals[1].items[0].portion).toBe('one bowl');expect(out.adherence).toBeUndefined()})});
